package transactions.transaction_manager.services;

import java.util.ArrayList;


//import transaction_manager.model.TransactionMasterDTO;

public interface I_TransactionMasterService
{
	/*
    abstract public ArrayList<TransactionMasterDTO> getAllTransactions();
	abstract public TransactionMasterDTO getTransactionByNo(Integer txnSeqNo);
	abstract ArrayList<TransactionMasterDTO> getTransactionsByType(Integer txnTypeSeqNo);
    abstract public TransactionMasterDTO newTransaction(TransactionMasterDTO transactionDTO);
    abstract void updateTransaction(TransactionMasterDTO transactionDTO);    
    abstract void deleteTransaction(Integer txnSeqNo);
    abstract void deleteAllTransactions();
    */
}